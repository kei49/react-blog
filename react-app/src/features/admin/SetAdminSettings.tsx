import React, { useState } from 'react';
import { connect } from 'react-redux';
import { setAdminSettings } from './adminSlice';

const mapDispatch = { setAdminSettings };

const SetAdminSettings = ({ setAdminSettings }) => {
  
}